+ зчитування числового індексу та базового посилання мережвого файлу у аргументі
- перевіряє валідність посилання
+ обхід захисних процедур cf
- підлаштування під нетипову поведінку цільового сайту
- Детекція контрмір цільового сайту: затримка, get-force відповіді, cf-захист
 - класифікація та підключення обхідних і специфічних технік для доступу до контенту та тіла відповіді на запит
 - підключення лише в разі необхідності усіх потрібних технік, в інакшому випадку requests
 - дати обирати користувачу чи потрібно намагатись очікувати відповідь, якщо час очікування сплив -> збільшення значення TIMEOUT
- Зчитування із буферу обміну в разі порожнього аргументу
- Збереження форматованої інформації отриманої із запитів у внутрішню пам'ять екземпляру
- Ведення журналів під конкретні сайти для продовження поряд із тілом скрипта, або у спец. папці.
- Синхронізація процесу роботи з даними і виводом:
 - переміщення замість нового запиту
 - не робити запит, а показувати вже збережені дані
 - Виділення світлозеленим поточного рядка замість повтору запиту, натомість неактивні залишаються темнозеленими
 - Збереження процесу виводу, щоб скрипт пам'ятав та знав скільки екрану він задіяв при виводі(або вводі користувачем) інформації
 - Очищення лише екранного буферу лише розміром інформації, а не усього рядка чи екрану. Мати обидва методи про всяк випадко
 • Проситься повноцінний невеликий фреймворк для консолі тут.
- Завантаження інформації із експортованих файлів певного формату(нашого).
- Пінг та реагування на відсутність з'єднання замість timeout, а також динамічний timeout
- Режим інкремент\декремент — не переміщатись у гору, якщо поточний декремент перевищуватиме поточний мінімальний індекс файлу, або після CTRL+E\R
- Зміна й імплементація іншого підходу обходу інформації та режимів вводу:
 + E – інкремент(продовжує із поточного місця в режимі інкремент)(ep?);
 - R – повернення(упирається в початковий, початок перебору)(return);
 + D – декремент(продовжує із поточного місця в режимі декремент)(down);
 + G – завантаження контенту(get);
 + C – переривання процесу(cencel);
 + Q – конвенційний вихід(quit);
 + O – дебаг команда: вивід більшої інформації про об'єкт;
 + © – ігнорування інших команд\символів вводу, щоб не крашити програму.
 - H – команда виділення оброблених посилань для пошуку подібних при подальшому аналізі
- Реагування на зміну CAPS_LOCK та мови системи в повідомленнях options-ів при виборі дії. Адаптивно підлаштовувати під зміннювані параметри операційної системи.
- CTRL+E – до упору вверх поточного списку(виведеного\обробленого)
- CTRL+D – до упору вниз поточного списку
- Збереження даних сесії ззовні програми - кеш, змінними, b64 або у файлі. 
+ Перевірка існуючого файлу в кінцевій директорії перед записом
+ Логіка для запобігання перезапису існуючого файлу

>>> VERSION_CALCULATION:

1.0.0 - All features implemented. Код приведено до ладу.
 ...
0.9.5 - *екстра* дозвіл на отримання сесійних даних(кукі) для персонального використання - ретрансляція роботи cffi_curl
0.9.4 - *екстра* детекція формату лінку, в разі відмінності - скасування роботи програми
0.9.3 - *екстра* зчитування лінку із буферу обміну
0.9.2 - *екстра* детекція аномалій та надання мінімальної можливості для вирішення
0.9.1 - *екстра* динамічне очікування та можливість інтерактивного погодження та\або налагодження із користувачем
0.9.0 - *екстра* аналітичний запит - середній час на підключення до кінцевого сервера(пінг, та підрахунок часу очікування) - потрібно для підлаштування часу очікування
0.8.4 - здатність завантажувати та поновлювати історію попередніх сесій(файлів вивантаження) без проблем із сумісністю
0.8.3 - можливість збереження історії обробки у файл, буфер обміну або інше джерело агрегації
0.8.2 - кращий обхід контекстів із історії для митєвого перемикання між вже обробленими, а не повторний запит кожен раз
0.8.1 - інтерактивний вивід інформації на екран із перезаписом для зниження навантаження на мережу та кращої взаємодії(без дозапису внизу)
0.8.0 - можливість збереження контексту обробленого запиту для формування історії (тільки в пам'яті робочого екземпляру)
0.7.2 - збереження даних сесії(куків) окремо для продовження чи спроби поновити при наступному запуску (тимчасові файли)
0.7.1 + інтеракція для вирішення конфлікту збереження контенту із джерела та поточного файлу
0.7.0 + попередження користувача про існуючий файл та перезапис
0.6.0 + первинний аналіз отриманої інформації із headers, для класифікації результату та типізації виводу (статус коди)
0.5.0 + ітеративний перебір файлів із зміщенням по індексу в лінку на вимогу користувача
0.4.1 + кольорова підсвідка тексту для покращення виділення інформації
0.4.0 + модуль взаємодії з користувачем: інтеракція, вивід, ввід (примітивний)
0.3.2 + реакція на блокування - переініціалізація сесії модуля мережевих запитів із cf-solver
0.3.1 + запис контенту у файл із запиту на вимогу користувача
0.3.0 + обхід блокування CF для отримання head запиту
0.2.0 + створення head-запит на основі лінку для отримання метаданих для аналізу
0.1.1 + виокремлювати префікс, суфікс, постфікс лінку та індекс файлу за допомогою regex
0.1.0 + брати й обробляти лінк dirrect-файлу із аргументу виклику
0.0.1 + project created, git initiated, simple py-template or empty program.